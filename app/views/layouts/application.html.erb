<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Banco Digital" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>



    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text x=%2250%22 y=%22.9em%22 font-size=%2290%22 text-anchor=%22middle%22>🏦</text></svg>">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="main-header"> 
      <div class="container">
        <div class="logo">
          Banco Digital
        </div>
        <nav>
          <% if logged_in? %>
            <span style="margin-right: 15px; vertical-align: middle;">Olá, <%= current_correntista.nome %>!</span>
            <%= button_to "Sair", logout_path, method: :delete, class: "nav-button" %> 
          <% else %>
            <%= link_to "Entrar", login_path, class: "nav-button" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main style="padding: 20px;"> 
      <% if notice %>
        <p style="color: green; border: 1px solid green; padding: 10px; margin-bottom: 10px;"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p style="color: red; border: 1px solid red; padding: 10px; margin-bottom: 10px;"><%= alert %></p>
      <% end %>

      <%= yield %> 
    </main>

  </body>
</html>
